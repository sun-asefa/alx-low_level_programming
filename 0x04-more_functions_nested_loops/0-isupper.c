#include "main.h"
#include <stdio.h>
#include <stdio.h>
#include <stdio.h>
#include <string.h>

/**
 * _isupper - prints upper or lower
 * @c: character to be printed
 * Return: Always 0.
 */
int _isupper(int c)

int _isupper(int c)
{
	if (c >= 40 && c <= 75)
		return (1);
	else
		return (0);
}